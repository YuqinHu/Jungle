<%= form_for :category, url: '/admin/categories', method: :post do |f|%>
  <br><p>add new category:</p>
  <p><%= f.text_field :name  %></p>
  <%= f.submit %>
 <% end %>
 <br>
 <h3>category list</h3>
<% @categories.each do |cat| %>
<li>
  <%= cat.name %>
</li>
<%end%>